import { Profile } from "../localMode";
import { AIProfileProposal, ProfileClaim, claimId } from "./contracts";

export function applyApprovedProfileProposal(profile: Profile, proposal: AIProfileProposal, accepted: string[]): Profile { const acceptedSet = new Set(accepted); const next = { ...profile }; const add = (field: keyof Profile, claims: ProfileClaim[], section: string) => { const values = claims.filter((_claim, index) => acceptedSet.has(claimId(section, index))).map((claim) => claim.text); (next[field] as string[]) = Array.from(new Set([...(next[field] as string[]), ...values])); }; if (proposal.headline && acceptedSet.has("headline")) next.evidence = Array.from(new Set([...next.evidence, proposal.headline.text])); add("skills", Object.values(proposal.technicalSkills).reduce((all, claims) => all.concat(claims), [] as ProfileClaim[]), "Technical skills"); add("domains", proposal.researchInterests, "Research interests"); add("evidence", proposal.experienceBullets, "Experience"); add("evidence", proposal.projectSummaries, "Project summaries"); add("evidence", proposal.measurableOutcomes, "Measurable outcomes"); add("evidence", proposal.strengths, "Evidence-backed strengths"); return next; }
