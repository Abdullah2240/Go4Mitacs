"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { buildLocalProfile } from "../lib/profile";
import { parseDocument, pasteDocument } from "../lib/documentParser";
import { clearKnowledgeBase, clearShortlist, DocumentRecord, forgetProviderKey, hasProviderKey, loadDocuments, loadLocalShortlist, loadProfile, loadReferenceAnnotations, LocalShortlistItem, Profile, ReferenceAnnotation, saveDocuments, saveLocalShortlist, saveProfile, saveReferenceAnnotations, Provider } from "../lib/localMode";
import { AppHeader } from "./components/AppHeader";
import { EvidenceWorkspace } from "./components/EvidenceWorkspace";
import { ProfileReview } from "./components/ProfileReview";
import { WorkspaceTabs } from "./components/WorkspaceTabs";
import { Project, StepId } from "./components/types";

const providerDefaults: Record<Provider, string> = { openrouter: "openai/gpt-4o-mini", gemini: "gemini-2.0-flash", huggingface: "Qwen/Qwen2.5-7B-Instruct" };
const steps = [["knowledge", "Evidence"], ["profile", "Profile"], ["results", "Matches"], ["shortlist", "Shortlist"]] as const;

export default function Home() {
  const [documents, setDocuments] = useState<DocumentRecord[]>([]);
  const [profile, setProfile] = useState<Profile | null>(null);
  const [shortlist, setShortlist] = useState<LocalShortlistItem[]>([]);
  const [annotations, setAnnotations] = useState<ReferenceAnnotation[]>([]);
  const [projects, setProjects] = useState<Project[]>([]);
  const [provider, setProvider] = useState<Provider>("openrouter");
  const [model, setModel] = useState(providerDefaults.openrouter);
  const [keyReady, setKeyReady] = useState(false);
  const [tab, setTab] = useState("knowledge");
  const [paste, setPaste] = useState("");
  const [status, setStatus] = useState("Add evidence to begin. The hosted site needs a network connection for public corpus routes.");
  const [busy, setBusy] = useState(false);
  const [filters, setFilters] = useState({ province: "", university: "", research: "" });
  const [width, setWidth] = useState("60");
  const [detail, setDetail] = useState<Project | null>(null);
  const [selected, setSelected] = useState<string[]>([]);
  const [consent, setConsent] = useState(false);
  const [settingsOpen, setSettingsOpen] = useState(false);

  useEffect(() => { setDocuments(loadDocuments()); setProfile(loadProfile()); setShortlist(loadLocalShortlist()); setAnnotations(loadReferenceAnnotations()); setKeyReady(hasProviderKey(provider)); }, [provider]);

  const combinedText = useMemo(() => documents.filter((d) => d.status === "ready" && d.kind !== "reference").map((d) => d.text).join("\n\n"), [documents]);
  const candidateDocuments = documents.filter((doc) => doc.kind !== "reference");
  const referenceDocuments = documents.filter((doc) => doc.kind === "reference");
  const corpusLabel = projects.length ? `${projects.length} shown from all 3,359 screened` : "3,359 public projects";

  function addDocs(files: FileList | File[], kind: "candidate" | "reference" = "candidate") { setBusy(true); Promise.all(Array.from(files).map((file) => parseDocument(file, kind))).then((parsed) => { const existing = new Set(documents.map((d) => d.hash)); const fresh = parsed.filter((d) => !d.hash || !existing.has(d.hash)); const next = [...documents, ...fresh]; setDocuments(next); saveDocuments(next); setStatus(`${fresh.length} ${kind === "reference" ? "private context " : ""}document${fresh.length === 1 ? "" : "s"} added locally. Duplicate content was skipped.`); }).finally(() => setBusy(false)); }
  function removeDoc(id: string) { const next = documents.filter((d) => d.id !== id); setDocuments(next); saveDocuments(next); const notes = annotations.filter((item) => item.source_document_id !== id); setAnnotations(notes); saveReferenceAnnotations(notes); setStatus("Document and any derived annotations were removed from this browser workspace."); }
  async function addPaste(event: FormEvent) { event.preventDefault(); if (!paste.trim()) return; const doc = await pasteDocument(paste); if (documents.some((d) => d.hash === doc.hash)) { setStatus("That pasted content is already in the workspace."); return; } const next = [...documents, doc]; setDocuments(next); saveDocuments(next); setPaste(""); setStatus("Pasted evidence added locally."); }
  function buildProfile() { if (!combinedText) { setStatus("Add at least one parsed evidence document first."); return; } const next = buildLocalProfile(documents); setProfile(next); saveProfile(next); setTab("profile"); setStatus("Local profile built from your evidence. Review it before matching."); }
  async function runMatch(event?: FormEvent) { event?.preventDefault(); if (!profile && !combinedText) { setStatus("Build a profile or add evidence first."); return; } setBusy(true); setTab("results"); setStatus("Screening the complete Mitacs corpus..."); try { const response = await fetch("/api/local/matches?limit=50", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ evidence_text: profile ? [...profile.skills, ...profile.tools, ...profile.domains, ...profile.methods, ...profile.evidence].join(" ") : combinedText, filters: { Province: filters.province, "Professor.UniversityName": filters.university, research_area: filters.research } }) }); const body = await response.json(); if (!response.ok) throw new Error(body.detail ?? "Matching failed"); setProjects(body.items ?? []); setStatus(`Full-corpus retrieval complete. ${body.considered ?? 3359} projects were screened.`); } catch (error) { setStatus(error instanceof Error ? error.message : "Matching failed. Your local evidence is preserved."); } finally { setBusy(false); } }
  function addShortlist(project: Project) { if (shortlist.some((x) => x.project_id === project.project_id)) return; const next = [...shortlist, { project_id: project.project_id, title: project.title, classification: project.group ?? "strong-fit", rank: shortlist.length + 1 }]; setShortlist(next); saveLocalShortlist(next); setStatus("Saved to the local shortlist."); }
  function classify(id: string, classification: string) { const next = shortlist.map((x) => x.project_id === id ? { ...x, classification } : x); setShortlist(next); saveLocalShortlist(next); }
  function toggleCompare(id: string) { setSelected((current) => current.includes(id) ? current.filter((item) => item !== id) : current.length >= 4 ? current : [...current, id]); }
  function exportMarkdown() { const body = shortlist.map((x, i) => `${i + 1}. **${x.title}** (${x.project_id}) - ${x.classification}`).join("\n"); const blob = new Blob([`# Mitacs shortlist\n\n${body || "No projects saved."}\n\n_Local browser export._`], { type: "text/markdown" }); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = "mitacs-shortlist.md"; a.click(); URL.revokeObjectURL(a.href); }
  function saveKey(event: FormEvent) { event.preventDefault(); const input = event.currentTarget.querySelector("input") as HTMLInputElement; if (input.value.trim()) { sessionStorage.setItem(`mitacs:provider-key:${provider}`, input.value.trim()); input.value = ""; setKeyReady(true); setStatus("Key held in sessionStorage only. No provider call was made."); } }
  function clearAll() { if (!window.confirm("Clear all local documents, profile, results, shortlist, and private context?")) return; clearKnowledgeBase(); clearShortlist(); setDocuments([]); setProfile(null); setProjects([]); setShortlist([]); setAnnotations([]); setStatus("Local workspace cleared."); }
  function saveAnnotation(project: Project, note: string) { const source = documents.find((doc) => doc.kind === "reference" && doc.status === "ready"); if (!source || !note.trim()) return; const next = [...annotations.filter((item) => item.project_id !== project.project_id), { project_id: project.project_id, note: note.trim(), source_document_id: source.id, context: "historical" as const }]; setAnnotations(next); saveReferenceAnnotations(next); setStatus("Historical context annotation saved locally. It does not affect ranking."); }
  const updateProfile = (field: keyof Profile, value: string) => { if (!profile) return; const next = { ...profile, [field]: value.split(",").map((x) => x.trim()).filter(Boolean) }; setProfile(next); saveProfile(next); };

  return <main className="shell">
    <AppHeader onSettings={() => setSettingsOpen(true)} onClear={clearAll} />
    <div className="workspace-shell">
      <WorkspaceTabs active={tab as StepId} steps={steps} onChange={(step) => setTab(step)} />
      <div className="statusbar" role="status"><span className={busy ? "pulse" : "status-mark"} />{status}</div>
      <section className="content">
        {tab === "knowledge" && <EvidenceWorkspace documents={documents} annotations={annotations} onAddDocuments={addDocs} onPaste={addPaste} paste={paste} setPaste={setPaste} onRemove={removeDoc} onBuild={buildProfile} canBuild={Boolean(combinedText)} />}
        {tab === "profile" && <ProfileReview profile={profile} onChange={updateProfile} onContinue={() => runMatch()} onBack={() => setTab("knowledge")} />}
        {tab === "results" && <section className="stage"><div className="section-head compact"><div><p className="eyebrow">Match results</p><h1>Choose the work worth a closer look.</h1><p>{corpusLabel}. Deterministic retrieval first, with optional AI review clearly separated below.</p></div><button className="button primary" onClick={() => runMatch()} disabled={!profile && !combinedText}>Refresh matches</button></div><div className="filter-row"><input aria-label="Filter by province" placeholder="Province" value={filters.province} onChange={(e) => setFilters({ ...filters, province: e.target.value })} /><input aria-label="Filter by university" placeholder="University" value={filters.university} onChange={(e) => setFilters({ ...filters, university: e.target.value })} /><input aria-label="Filter by research area" placeholder="Research area keyword" value={filters.research} onChange={(e) => setFilters({ ...filters, research: e.target.value })} /><button className="button secondary" onClick={() => runMatch()}>Apply filters</button></div><div className="ai-note"><div><strong>AI reranking is coming soon</strong><p>When enabled, this will be an optional, consented review of a bounded set. It will not change the 3,359-project search.</p></div><label>Review width<select value={width} onChange={(e) => setWidth(e.target.value)} disabled><option>60</option><option>120</option><option>240</option></select></label><button className="button secondary" disabled>Unavailable</button></div>{projects.length === 0 ? <div className="empty large"><strong>Run a match to see the complete corpus ranked.</strong><span>Your evidence stays local while the public project index is screened.</span></div> : <div className="result-list">{projects.map((project, index) => <article className="result-row" key={project.project_id}><div className="rank">{String(index + 1).padStart(2, "0")}</div><div className="result-main"><div className="result-title"><h2>{project.title}</h2><span className={`tag ${project.group}`}>{project.group}</span></div><p className="project-meta">{project.metadata?.["Professor.UniversityName"] || "University not listed"} <span>·</span> {project.metadata?.["Professor.FirstName"]} {project.metadata?.["Professor.LastName"]} <span>·</span> {project.metadata?.Province}</p><p className="result-preview">{project.text_preview}</p><div className="result-footer"><div className="chips">{(project.matched_evidence ?? []).slice(0, 5).map((x) => <span key={x}>{x}</span>)}</div><label className="compare-check"><input type="checkbox" checked={selected.includes(project.project_id)} onChange={() => toggleCompare(project.project_id)} disabled={!selected.includes(project.project_id) && selected.length >= 4} /> Compare</label></div></div><div className="result-side"><div><strong>{project.score ?? 0}</strong><span>/ 100</span></div><button className="link-button" onClick={() => setDetail(project)}>View details</button><button className="link-button" onClick={() => addShortlist(project)}>Save locally</button></div></article>)}</div>}</section>}
        {tab === "shortlist" && <section className="stage"><div className="section-head compact"><div><p className="eyebrow">Shortlist</p><h1>Turn good matches into a plan.</h1><p>{shortlist.length} saved locally. Classify projects, compare up to four, and export when ready.</p></div><button className="button secondary" onClick={exportMarkdown}>Export Markdown</button></div>{shortlist.length === 0 ? <div className="empty large"><strong>Your shortlist is empty.</strong><span>Save projects from Matches to bring them here.</span><button className="button secondary" onClick={() => setTab("results")}>Back to Matches</button></div> : <div className="shortlist-list">{shortlist.map((item) => <div className="short-row" key={item.project_id}><span className="rank">{String(item.rank).padStart(2, "0")}</span><div><h2>{item.title}</h2><p className="fine">Project {item.project_id}</p></div><select aria-label={`Classification for ${item.title}`} value={item.classification} onChange={(e) => classify(item.project_id, e.target.value)}><option>ambitious</option><option>strong-fit</option><option>reliable</option><option>confirmed</option></select></div>)}</div>}<section className="compare-panel"><div className="block-heading"><div><h2>Compare selected projects</h2><p className="fine">Select up to four from Matches. The comparison stays in this browser.</p></div><span className="count-badge">{selected.length} of 4 selected</span></div>{selected.length ? <div className="compare-columns">{selected.map((id) => { const p = projects.find((x) => x.project_id === id); return <div className="compare-card" key={id}><button className="remove-compare" onClick={() => toggleCompare(id)} aria-label={`Remove ${p?.title ?? id} from comparison`}>×</button><h3>{p?.title ?? `Project ${id}`}</h3><dl><div><dt>University</dt><dd>{p?.metadata?.["Professor.UniversityName"] ?? "Not listed"}</dd></div><div><dt>Supervisor</dt><dd>{p?.metadata?.["Professor.FirstName"]} {p?.metadata?.["Professor.LastName"]}</dd></div><div><dt>Province</dt><dd>{p?.metadata?.Province ?? "Not listed"}</dd></div><div><dt>Evidence</dt><dd>{(p?.matched_evidence ?? []).join(", ") || "No exact terms"}</dd></div><div><dt>Gaps</dt><dd>{(p?.missing_evidence ?? []).join(", ") || "Review manually"}</dd></div></dl></div>; })}</div> : <p className="empty compare-empty">No projects selected yet. Use Compare on a result row.</p>}</section></section>}
      </section>
    </div>
    {detail && <div className="modal-backdrop" onClick={() => setDetail(null)}><div className="modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setDetail(null)}>Close</button><p className="eyebrow">Project {detail.project_id}</p><h2>{detail.title}</h2><p className="modal-intro">{detail.text_preview}</p><div className="detail-grid"><div><span>Supervisor</span><strong>{detail.metadata?.["Professor.FirstName"]} {detail.metadata?.["Professor.LastName"]}</strong></div><div><span>University</span><strong>{detail.metadata?.["Professor.UniversityName"]}</strong></div><div><span>Province</span><strong>{detail.metadata?.Province}</strong></div><div><span>Language</span><strong>{detail.metadata?.LanguageUsed}</strong></div></div>{annotations.find((item) => item.project_id === detail.project_id) && <div className="history-note"><strong>Historical context</strong><p>{annotations.find((item) => item.project_id === detail.project_id)?.note}</p><span className="fine">This private annotation is not a prediction or ranking signal.</span></div>}<form onSubmit={(event) => { event.preventDefault(); const input = event.currentTarget.elements.namedItem("history-note") as HTMLInputElement; saveAnnotation(detail, input.value); input.value = ""; }} className="annotation-form"><label htmlFor="history-note">Add private historical context</label><input id="history-note" name="history-note" placeholder="Source-backed note from the selected private context" disabled={!referenceDocuments.some((doc) => doc.status === "ready")} /><button className="button secondary" disabled={!referenceDocuments.some((doc) => doc.status === "ready")}>Save locally</button></form>{detail.source_url && <a href={detail.source_url} target="_blank" rel="noreferrer" className="button secondary">Open on Globalink</a>}</div></div>}
    {settingsOpen && <div className="modal-backdrop" onClick={() => setSettingsOpen(false)}><div className="modal settings-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={() => setSettingsOpen(false)}>Close</button><p className="eyebrow">Optional settings</p><h2>Provider configuration</h2><p className="modal-intro">Provider settings are hidden by default and are not used for live AI reranking in this deployment. Keys remain in sessionStorage for this browser session only.</p><form onSubmit={saveKey} className="settings-form"><label>Provider<select value={provider} onChange={(e) => { const value = e.target.value as Provider; setProvider(value); setModel(providerDefaults[value]); }}><option value="openrouter">OpenRouter</option><option value="gemini">Gemini</option><option value="huggingface">Hugging Face</option></select></label><label>Model<input value={model} onChange={(e) => setModel(e.target.value)} /></label><label>Session-only key<input aria-label="Provider API key" type="password" placeholder={keyReady ? "Key held for this session" : "Optional provider key"} autoComplete="off" /></label><div className="settings-actions"><button className="button primary">Hold key for session</button>{keyReady && <button className="link-button" type="button" onClick={() => { forgetProviderKey(provider); setKeyReady(false); }}>Forget key</button>}</div></form><div className="consent-summary"><strong>Consent boundary</strong><p>No content is sent to a provider without a separate, unambiguous confirmation for that action.</p><button className="text-action" onClick={() => { setSettingsOpen(false); setConsent(true); }}>Read consent policy</button></div></div></div>}
    {consent && <div className="modal-backdrop"><div className="modal consent"><button className="modal-close" onClick={() => setConsent(false)}>Close</button><p className="eyebrow">Consent policy</p><h2>Before any provider request</h2><p>This app would name the selected provider and model, list the exact categories of content being sent, explain that the key and content are used only for that action, and let you cancel.</p><p>Live AI reranking is not available in this deployment. No provider request has been made.</p><button className="button primary" onClick={() => setConsent(false)}>Done</button></div></div>}
    <footer className="footer-note">Go4Mitacs is an independent project-matching tool and is not affiliated with or endorsed by Mitacs.</footer>
  </main>;
}
