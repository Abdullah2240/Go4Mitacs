import { FormEvent } from "react";
import { Provider } from "../../lib/localMode";
type Props = { provider: Provider; model: string; keyReady: boolean; onProvider: (provider: Provider) => void; onModel: (model: string) => void; onSaveKey: (event: FormEvent<HTMLFormElement>) => void; onForgetKey: () => void; onClose: () => void; onConsent: () => void; onSmokeTest: () => void };
export function SettingsDialog({ provider, model, keyReady, onProvider, onModel, onSaveKey, onForgetKey, onClose, onConsent, onSmokeTest }: Props) { return <div className="modal-backdrop" onClick={onClose}><div className="modal settings-modal" role="dialog" aria-modal="true" aria-labelledby="settings-title" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close settings">Close</button><p className="eyebrow">Optional settings</p><h2 id="settings-title">Provider configuration</h2><p className="modal-intro">Keys remain in sessionStorage. Saving a key makes no provider call. Any smoke test is manual, consent-gated, and reports its status without logging the key.</p><form onSubmit={onSaveKey} className="settings-form"><label>Provider<select value={provider} onChange={(e) => onProvider(e.target.value as Provider)}><option value="openrouter">OpenRouter</option><option value="gemini">Gemini</option><option value="huggingface">Hugging Face</option></select></label><label>Model<input value={model} onChange={(e) => onModel(e.target.value)} /></label><label>Session-only key<input aria-label="Provider API key" type="password" placeholder={keyReady ? "Key held for this session" : "Optional provider key"} autoComplete="off" /></label><div className="settings-actions"><button className="button primary">Hold key for session</button>{keyReady && <button className="link-button" type="button" onClick={onForgetKey}>Forget key</button>}</div></form><div className="consent-summary"><strong>Consent boundary</strong><p>No content is sent to a provider without a separate, unambiguous confirmation.</p><button className="text-action" onClick={onConsent}>Read consent policy</button>{keyReady && <button className="button secondary" onClick={onSmokeTest}>Run manual provider smoke test</button>}</div></div></div>; }
