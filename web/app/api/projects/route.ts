import { NextRequest, NextResponse } from "next/server";
import { filterProjects, publicCorpus } from "../../../lib/publicCorpus";

const MAX_LIMIT = 100;
export function GET(request: NextRequest) { const params = request.nextUrl.searchParams; const filtered = filterProjects(publicCorpus.projects, params); const limit = Math.min(MAX_LIMIT, Math.max(1, Number(params.get("limit") || 25) || 25)); const offset = Math.max(0, Number(params.get("offset") || 0) || 0); const items = filtered.slice(offset, offset + limit).map((project) => ({ project_id: project.id, title: project.title, metadata: { Province: project.province, LanguageUsed: project.language, "Professor.FirstName": project.supervisor.first_name, "Professor.LastName": project.supervisor.last_name, "Professor.UniversityName": project.university, "Professor.CampusName": project.campus, StartDate: project.start_date }, source_url: project.source_url, source_retrieved_at: project.source_retrieved_at })); return NextResponse.json({ items, total: filtered.length, limit, offset, corpus_version: publicCorpus.corpus_version }, { headers: { "Cache-Control": "public, max-age=300, s-maxage=3600, stale-while-revalidate=86400" } }); }
