# No environment variables are required for the active Next.js/Vercel build.
# Provider keys are entered per browser session and are never configured here.
# Historical Python/Supabase tooling remains archived and is not needed to run web/.
